// Environment configuration
// IMPORTANT: Replace with your actual Anthropic API key
// In production, use environment variables or secure storage

export const ANTHROPIC_API_KEY = process.env.EXPO_PUBLIC_ANTHROPIC_API_KEY || '';

// Set to true to use simulated responses (for testing without API)
export const USE_SIMULATED_RESPONSES = false;
